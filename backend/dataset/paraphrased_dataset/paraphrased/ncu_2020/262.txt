資料集的情感標籤以數字(+1, 0, -1)表示，實驗使用一位有效編碼將其轉換為向量，以利情感極性判斷、預測及損失函數計算。向量維度等於情感類別數(三類)，每個位元對應一個類別。例如，正向(+1)編碼為[0,1,0]，中性(0)為[1,0,0]，負向(-1)為[0,0,1]。向量中的每個元素可視為對應類別的預測機率。例如，預測結果[0.2, 0.7, 0.1]代表該評論最可能為正向。損失函數計算預測結果(通常為機率值)與標準答案(一位有效編碼向量)的差距，作為模型訓練的依據，以調整參數並提升預測準確度。
