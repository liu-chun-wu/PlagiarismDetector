最後一步就是將檢測窗口中所有重疊的塊進行HOG 特徵的收集，並將它們結合 成最終的特徵向量供分類使用。本研究透過Opencv 的resize 函數，將擷取出來的圖 像寬高固定在148 x 226，每10 x 10 像素組成一個cell，4 x 4 個cell 組成一個塊 (block)，方向區則分為9 個bins，因此一張圖像總共會有1881 個特徵，最後將提取 的HOG 特徵透過PCA 降維，再輸入到SVM 分類器。
