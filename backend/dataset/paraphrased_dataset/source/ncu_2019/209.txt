此部分為組合單一樣本選取與單一離散化方法，並採用先使用樣本選取，後 使用離散化的先後順序進行實驗。首先，與Baseline 相同，將本論文使用的資料 集分成80%訓練資料與20%測試資料。第二，分別使用三種樣本選取方法對訓練 資料集做篩選，篩選出具有代表性樣本的資料，此時經過樣本選取後的訓練資料， 所剩下的樣本數會較篩選前少。第三，將樣本選取後的資料集進行離散化，並且 記錄其切割點，接著再將測試資料集依據訓練資料集的切割點進行離散化。第四， 將離散化後的訓練資料集放入KNN 建立模型，並把該折完成離散化後的測試資 料集放入模型做測試，得到該模型的評估指標，並經過五折交叉驗證得到五次實 驗的平均評估指標，完成先樣本選取後離散化的實驗流程。依據本實驗選用的方 法，可以分成（DROP3 + MDLP + KNN）、（DROP3 + ChiMerge + KNN）、、（IB3 + MDLP + KNN）、（IB3 + ChiMerge + KNN）、（GA + MDLP + KNN）、與（GA + ChiMerge + KNN）等6 種先樣本選取再執行離散化的搭配方式。此部分的實驗 流程如下圖3-6 所示。 圖3-7 先執行樣本選取再執行離散化（IS + D）流程圖 Data Train Data Test Data Train Data Train Data Test Data 依據相同cut point 作切割 Classifier Model 評估指標 Instance Selection DROP3 IB3 GA Discretizers MDLP ChiMerge KNN 下頁圖3-7 為本論文實驗（IS + D）流程的虛擬碼（Pseudo Code），可以藉由 此圖所示之虛擬碼，以程式的角度與運作流程，來更加了解本論文中，實驗所使 用的（IS + D）數據取得流程。虛擬碼中的i 代表該步驟正在處理第幾折步驟的 資料集，在樣本選取（IS）的過程中，會分別從GA、DROP3、與IB3 三種樣本 選取演算法中選用一種方法，並且處理該折的訓練資料集，得到樣本數量縮減過 後的訓練資料集，接著再從MDLP 與ChiMerge 中選用其中一種離散化方法，將 縮減過後的訓練資料集做離散化，並把測試資料集依照該折數訓練資料集的切點 做離散化。最後把該折的訓練資料集放入選用的分類器中做訓練得到模型，然後 將該折的測試資料集放入模型檢驗效能，並重覆步驟直到該資料集的五個折數皆 做完評估指標計算，再將五個評估指標值做平均，即可得到該資料集在（IS + D） 流程中所擁有的平均評估指標。 Algorithm: IS + D Input: A set of dataset S Output: Evaluating indicator e 1. For each dataset s in S 2.   Divide s into 80% Train Data & 20% Test Data by using 5-fold cross validation 3.     For i from 1 to 5 4.       Use the instance selection algorithms to process train i 5.       Get the optimal instances and reduce the original train i 6.       Get the reduce training dataset train i 7.       Use the discretization algorithms to process train i 8.       Record the cut points p on train i 9.       Get the discrete dataset train i 10.       Use the same discretization algorithms & p to process test i 11.       Get the discrete dataset test i 12.       Put train i into classifier to train the model  //Classifier: 1-NN 13.       Put test i into the model to evaluate e 14.       Get & record fold i's e 15.   Get 5 folds' e 16.   Total the 5 folds' e and divide by 5 17.   Get the average e of dataset s 18.   Output the average e of dataset s 圖3-8 本研究（IS + D）流程之虛擬碼
