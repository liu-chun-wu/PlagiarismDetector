圖 七為將四種特徵分成四個欄位交由四個text-CNN 學習，其中除了評論 為較長的文字特徵將每筆欄位長度限制為500 之外，其餘三個文字特徵長度限 制都設置為20，每個特徵都透過與圖 五架構雷同的方法再透過Concatenate 層 結合各自學習到的特徵平均後取最大值做分類。 圖 七、Text-CNN(M6)模型結構圖 圖 八為將四種特徵分成四個欄位交由四個Bi-LSTM 學習，每個特徵再透 過Concatenate 層結合各自學習到的特徵平均後取最大值做分類。 圖 八、Bi-LSTM(M7)模型架構圖 圖 九為Ensemble Model(M8)我們認為評論作為較長的文字特徵，透過 text-CNN 取得進階的文字特徵後，再交由Bi-LSTM 學習序列關係的情緒特 徵，更能正確的將評論分類到正確的類別。除了將較長的評論透過text-CNN 與 Bi-LSTM 的模型分析之外，我們將其他較短的三個文字特徵用text-CNN 作分 類。 圖 九、Ensemble Model(M8)模型架構圖 圖十為Ensemble Model(M9)我們除了一樣將較長的文字特徵評論，透過 text-CNN 取得進階的文字特徵後，再交由Bi-LSTM 學習序列關係的情緒特徵 之外，將其餘三個較短的文字特徵交由Bi-LSTM 分類與M8 做比較。 圖 十、Ensemble Model(M9)模型架構圖 第四章 實驗結果
