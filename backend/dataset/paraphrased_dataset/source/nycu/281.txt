1. 欄位Y=1為異常、欄位Y=0為正常。 2. 我們將資料切分為訓練與測試資料，取test_size=0.3不參與訓練，訓練資料是 用來建立模型。 3. 我們建模用訓練集裡面其中y=0的樣本去建模（X_train = X_train[X_train.Y1 == 0]），因此y 欄就可以移除（X_train = X_train.drop([‘Y1’], axis=1)）。 4. 我們要驗證模型是拿測試集去驗證，先把測試集的正確答案放起來到 y_test ，（y_test = X_test['Y1']） 5. 驗證模型只餵測試集的feature(即X)，（X_test = X_test.drop([‘Y1’], axis=1)）， 讓模型去預測出 y‘。 6. 接下來有y 跟 y‘。 7. 就可以拿來比對標準答案y 跟預測出來的y'了。 8. 接著以confusion matrix 跟accuracy 進行模型評估。 9. 圖5架構即是簡化後的資料說明，可以了解是取y=0的正常樣本來建立模型， 並使用測試集去預測y’並比對結果是否正確。 圖 5 資料說明架構圖
